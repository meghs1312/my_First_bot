# This file contains the different endpoints your bot can use.

# Server which runs your custom actions.
# https://rasa.com/docs/rasa/custom-actions

action_endpoint:
  url: "http://localhost:5055/webhook"

# (Optional) Tracker store, uncomment if you're using Redis, Mongo, etc.
# tracker_store:
#     type: redis
#     url: localhost
#     port: 6379
#     db: 0
#     password: null
#     use_ssl: false

# Event broker (optional, for analytics or streaming events)
# event_broker:
#   url: localhost
#   username: username
#   password: password
#   queue: queue
